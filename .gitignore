__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.DS_Store

# personal subscription history (use plan.example.json as the template)
plan.json

# personal / machine-local steering context (not for the open-source repo)
CLAUDE.local.md
.claude/settings.local.json

# personal calibration inputs (chat exports, quota CSVs, screenshots) — NEVER commit.
# Everything under gitignored-data/ is yours-only; only the README documenting the layout is tracked.
gitignored-data/*
!gitignored-data/README.md
